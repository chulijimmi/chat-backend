{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Script: develop",
            "request": "launch",
            "command": "npm run develop",
            "cwd": "${workspaceFolder}"
        }
    ]
}